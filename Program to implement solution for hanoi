	
/*
Name : Nishanth R
Date : 29.11.2021
Description : Program to implement solution for hanoi
Input : Enter the number of disks: 3
Output:
Move disk 1 from rod A to rod C
Move disk 2 from rod A to rod B
Move disk 1 from rod C to rod B
Move disk 3 from rod A to rod C
Move disk 1 from rod B to rod A
Move disk 2 from rod B to rod C
Move disk 1 from rod A to rod C
*/
#include<stdio.h>
void tower_of_hanoi(int n, char from, char to, char aux) // intilizing function
{
        if (n == 1) // condition for disk to move
        {
                printf("Move disk %d from rod %c to rod %c\n",n, from,to);
                return;
        }
        tower_of_hanoi(n-1,from,aux,to); // function call
        printf("Move disk %d from rod %c to rod %c\n", n, from,to);
        tower_of_hanoi(n-1,aux,to,from); // function call
}

int main()
{
        char choice;
        do
        {

                int n; // Number of disks
                printf("Enter the number of disks:");
                scanf("%d",&n);
                if (n > 0)
                {
                        tower_of_hanoi(n,'A','C','B');  // A, B and C are names of rods
                }
                else
                {
                        printf("Error:Enter only positive numbers\n");
                }
                printf("Do you want to continue (y/Y)?:");
                scanf(" %c",&choice);
        }
        while (choice == 'y' || choice == 'Y' );
}
